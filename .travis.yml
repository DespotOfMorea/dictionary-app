language: java
script: mvn clean install -Dtest=*testGetByTerm,!FromTestTermDao
env:
  - MYSQL_HOST=127.0.0.1 MYSQL_USER=root
services:
  - mysql
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS geodictionary;'
